# 参数

- 全局共享字典
- 由键值组成
- 可实现动态监控

## 参数优先级

- 命令行参数（最高优先级）：例如 ros2 run pkg node --ros-args -p turtlename:=my_turtle。
- Launch 文件中指定的参数：在 .launch.py 中通过 parameters=[...] 传入的 YAML 文件或字典。
- YAML 参数文件中的值：文件里写死的值。
- declare_parameter 中的默认值（最低优先级）：即你代码中的 "turtle"。
